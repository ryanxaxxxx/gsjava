# Profile de Produção (SQL Server)
# Este perfil deve ser usado em produção/cloud (Azure DevOps)

spring.application.name=Green-way
spring.messages.basename=messages
spring.messages.encoding=UTF-8

# Datasource Configuration
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.url=${DB_URL}

# JPA Configuration
# Configuração para mostrar o SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto do SQL Server
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Não recriar o banco a cada inicialização (Flyway cuida disso)
spring.jpa.hibernate.ddl-auto=none

# Exibir SQL gerado pelo Hibernate
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.default_schema=dbo

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-migration-naming=true
spring.flyway.locations=classpath:db/migration

# JWT Configuration (deve ser configurado via variáveis de ambiente em produção)
jwt.secret=${JWT_SECRET:8f1c56c4b8a9e1ba3d1f8a49f3c0b1223b8379fd89237b1234abf89123ce1122}
jwt.expiration=${JWT_EXPIRATION:3600000}

# RabbitMQ Configuration (deve ser configurado via variáveis de ambiente em produção)
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

# ORS API Configuration
ors.api.key=${ORS_API_KEY:eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6IjUyNDY5Zjk5MzA4NDQyM2RiMTYxMmYyYzViZWNkMDYxIiwiaCI6Im11cm11cjY0In0=}
ors.api.url=${ORS_API_URL:https://api.openrouteservice.org/v2/directions/driving-car}

